## v2.3.5 Release notes

  * Added functionality:
    - Include shared, generic domain parsing and validation.
      - Add get_domain function to dialog_wrapper.
      - Specifically added to resolve #2135 (WordPress specific) but will allow
        consistent domain parsing/validation across all appliances.
